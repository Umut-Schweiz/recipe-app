import { ManageDropdownDirective } from './manage-dropdown.directive';

describe('ManageDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new ManageDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
